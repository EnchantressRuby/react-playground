//? Header Component
//* arrow func. with def export
//* import = import .... from "./..."

const Header = () => {
    // JS
    const message = "Hello world"
    return(
        // JSX
        <header>
            <h1>{message}</h1>
        </header>
    );
};

export default Header