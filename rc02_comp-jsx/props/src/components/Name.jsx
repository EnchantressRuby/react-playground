const Name = ({ name }) => {
    return (
        <div>
            <h2 style={{ padding: "1rem", fontWeight: "bold", fontSize: "2rem" }}>Name: {name}</h2>
        </div>
    )
}

export default Name;